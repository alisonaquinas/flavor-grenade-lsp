# src/vault/**tests**/

For vault changes, add tests for Windows and POSIX path shapes where relevant.
Keep filesystem effects confined to test-created directories and never read
sibling repositories.

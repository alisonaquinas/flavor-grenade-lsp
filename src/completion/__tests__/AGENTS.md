# src/completion/**tests**/

When completion behavior changes, cover both positive matches and nearby
non-trigger text. Keep tests independent from global vault state and prefer
small inline documents over broad fixtures.

# src/parser/**tests**/

Add parser tests before changing tokenization rules. Include opaque-region
counterexamples when a syntax feature could appear inside code, math, comments,
or Templater-style blocks.

# Inference-Only Markdown Flavor Fixtures

These fixtures intentionally omit Flavor Grenade project config markers. They
model the Auto Detect path where Flavor Grenade must infer a flavor from strong
local syntax and bounded workspace context.

The current implementation may still fall back to CommonMark. These samples are
the smoke-test inputs for the planned inference implementation.

---
id: "BUG-043"
title: "Refresh stale LSP unit harnesses for flavor propagation"
type: bug
status: done
severity: medium
phase: 20
parent: "FEAT-043"
created: "2026-05-13"
updated: "2026-05-13"
tags: [tickets/bug, "phase/20", tests]
aliases: ["BUG-043"]
---

# Refresh Stale LSP Unit Harnesses For Flavor Propagation

> [!SUCCESS] `BUG-043` - Bug - Phase 20 - Severity: `medium` - Status: `done`

## Finding

`bun test src/` failed after the Phase 20 implementation because older unit
test harnesses still assumed pre-flavor constructor/API shapes:

- `src/lsp/lsp.module.test.ts` manually instantiates `LspModule`; the new
  `ConfigurationHandler` constructor argument shifted later mocks.
- `src/lsp/handlers/__tests__/did-open.handler.test.ts` and
  `src/lsp/handlers/__tests__/did-change.handler.test.ts` still mock
  `VaultDetector.detect` while the handlers now use per-document
  `detectFresh`.

## Fix Plan

- Update manual `LspModule` test wiring to include a configuration handler
  mock before file-operation mocks.
- Update didOpen/didChange vault detector mocks to expose `detectFresh`.
- Preserve existing behavioral assertions while accepting the added parser
  context argument.

## Workflow Log

> [!INFO] Opened - 2026-05-13
> Ticket opened before fixing the `bun test src/` findings.

> [!SUCCESS] Fixed - 2026-05-13
> Updated the LSP module, didOpen, and didChange unit harnesses. `bun test src/`
> now passes.

---
id: "BUG-042"
title: "Extension host test blocked by VS Code update mutex"
type: bug
status: triaged
priority: medium
severity: medium
phase: "18"
introduced-in: "Phase 18 verification"
created: "2026-05-13"
updated: "2026-05-13"
dependencies: []
tags: [tickets/bug, "phase/18", verification, extension, host-test]
aliases: ["BUG-042"]
---

# Extension Host Test Blocked By VS Code Update Mutex

> [!INFO] `BUG-042` · Bug · Phase 18 · Severity: `medium` · Priority: `medium` · Status: `triaged`

## Summary

`npm run test:host` failed during Phase 18 local verification because the VS
Code test runtime could not launch while the Windows `vscode-updating` mutex was
held.

## Reproduction Steps

1. Run `npm run test:host` from `extension/`.
2. Wait for `@vscode/test-electron` to resolve and launch VS Code.
3. Observe that VS Code exits before extension-host tests run.

**Minimal reproducing vault content:**

```markdown
N/A - the failure occurs before the extension-host fixture opens.
```

**LSP method / trigger:** extension-host verification command.

## Expected Behaviour

The extension-host gate launches VS Code and runs the host regression suite.

## Actual Behaviour

VS Code reported `Code is currently being updated. Please wait for the update
to complete before launching.` after waiting roughly 30 seconds on the
`vscode-updating` mutex.

## Violated Requirement

| Planguage Tag | Gist | Source File |
|---|---|---|
| `Extension.Tests.HostCoverage` | Extension host behavior must be covered by executable tests. | [[docs/requirements/functional/vscode-extension-parity]] |
| `CICD.Workflow.PRGate` | The repository test gate must be reliable and executable. | [[docs/requirements/ci-cd]] |

## Linked BDD Scenario

| Feature File | Scenario Title | Gap? |
|---|---|---|
| `docs/bdd/features/vscode-extension.feature` | Extension activation on markdown file open | No - BDD passed; this finding is in the extension-host gate. |

## Linked Test

| Test File | Type | Status |
|---|---|---|
| `extension/scripts/run-host-tests.mjs` | Host verification | failed locally |

## Proposed Fix

Triage whether this is a transient local VS Code updater lock or a repeatable
host-test harness issue. If transient, rerun after the external lock clears and
close as out of scope for repository code. If repeatable, add a harness retry or
preflight check before the host test launch.

## Regression Guard

- [ ] Host test command rerun after ticket creation.
- [ ] If code changes are needed, add a regression test before the fix.

## Workflow Log

> [!INFO] Opened - 2026-05-13
> Found during Phase 18 extension verification after compile, unit,
> marketplace asset, and package-target checks passed. Status: `open`.

> [!NOTE] Triaged - 2026-05-13
> Root cause confirmed: active `CodeSetup-stable` processes were holding the
> VS Code update mutex, so the test Electron runtime exited before extension
> host tests could start. Status: `triaged`.

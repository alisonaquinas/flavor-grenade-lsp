---
id: "BUG-017"
title: "Enforce frontmatter YAML size and alias limits"
type: bug
status: open
priority: "high"
severity: "high"
phase: "18"
introduced-in: "phase-03-ofm-parser"
created: "2026-05-08"
updated: "2026-05-08"
dependencies: []
tags: [tickets/bug, "phase/18", security, parser]
aliases: ["BUG-017"]
---

# Enforce Frontmatter YAML Size And Alias Limits

> [!INFO] `BUG-017` · Bug · Phase 18 · Severity: `high` · Priority: `high` · Status: `open`

## Summary

`FrontmatterParser` calls `yamlLoad()` on the entire frontmatter block with
`CORE_SCHEMA`, but it does not enforce the documented 64 KB size limit or alias
cap before parsing.

---

## Reproduction Steps

1. Open a vault document whose frontmatter body exceeds 64 KB.
2. Include many YAML anchors or aliases.
3. Observe that parsing is attempted instead of being rejected as malformed frontmatter.

**Minimal reproducing vault content:**

```markdown
---
a: &a ["x"]
b: *a
---
# Alpha
```

**LSP method / trigger:** `textDocument/didOpen`, initial vault scan

---

## Expected Behaviour

Oversized or over-aliased YAML frontmatter is treated as malformed frontmatter
without unbounded parser work.

---

## Actual Behaviour

The parser slices the full YAML block and passes it directly to `yamlLoad()`.

---

## Violated Requirement

| Planguage Tag | Gist | Source File |
|---|---|---|
| `Security.Parser.YAMLLimits` | YAML uses alias cap 50, size limit 64 KB, and safe mode | [[requirements/security/parser-safety]] |

---

## Linked BDD Scenario

| Feature File | Scenario Title | Gap? |
|---|---|---|
| `docs/bdd/features/vault-detection.feature` | Oversized frontmatter is reported as malformed without crashing | Yes |

---

## Linked Test

| Test File | Type | Status |
|---|---|---|
| `src/parser/__tests__/frontmatter-parser.test.ts` | Unit | 🔴 planned |

---

## Affected ADRs

| ADR | Relevance |
|---|---|
| [[adr/ADR012-parser-safety-policy]] | Defines parser resource budgets and safe YAML handling |

---

## Proposed Fix

Reject frontmatter above 64 KB before YAML parsing, configure supported
`js-yaml` alias protections if available, and add an explicit post-parse alias
budget guard if the library option cannot express the policy.

---

## Regression Guard

- [ ] RED oversized-frontmatter test.
- [ ] RED alias-budget test.
- [ ] `Security.Parser.YAMLLimits` matrix row updated to `✅ passing`.

---

## Workflow Log

> [!INFO] Opened — 2026-05-08
> Audit confirmed the documented YAML limits are not enforced. Status: `open`.
